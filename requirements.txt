# torch is pre-installed on Colab with CUDA support — do not add it here
transformers>=4.45
peft>=0.13
accelerate>=0.34
bitsandbytes>=0.43
datasets>=2.21
sentencepiece>=0.2

# Training (unsloth-zoo is pulled in automatically as a dependency)
# Note: on Colab, prefer: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
unsloth>=2025.1

# Inference
vllm>=0.6.3

# Judge (OpenAI SDK; also used for local_vllm OpenAI-compatible endpoint)
openai>=1.50
